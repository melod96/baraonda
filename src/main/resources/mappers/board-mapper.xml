<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">
	<resultMap type="com.kh.baraonda.board.model.vo.Board" id="boardResultSet">
		<id property="board_no" column="BOARD_NO" />
		<result property="board_type" column="BOARD_TYPE" />
		<result property="writing_type" column="WRITING_TYPE" />
		<result property="board_title" column="BOARD_TITLE" />
		<result property="board_content" column="BOARD_CONTENT" />
		<result property="member_no" column="MEMBER_NO" />
		<result property="board_date" column="BOARD_DATE" />
		<result property="modify_date" column="MODIFY_DATE" />
		<result property="board_status" column="BOARD_STATUS" />
		<result property="board_count" column="BOARD_COUNT" />
		
		<!-- <association javaType="Member" property="Member">
			<id column="member_no" property="member_no"/>
			<result column="nick_name" property="nick_name"/>
		</association> -->

		
	</resultMap>

	<select id="listAll" resultMap="boardResultSet">
		SELECT B.BOARD_NO, B.BOARD_TYPE, B.WRITING_TYPE, B.BOARD_TITLE, B.BOARD_CONTENT,
			   B.MEMBER_NO, B.BOARD_DATE, B.MODIFY_DATE, B.BOARD_STATUS, B.BOARD_COUNT
		FROM BOARD B
        JOIN MEMBER M ON(B.MEMBER_NO = M.MEMBER_NO)
        WHERE B.WRITING_TYPE = 11
		ORDER BY B.BOARD_NO DESC
	</select>
	
	<!-- <select id="listAll" resultMap="boardResultSet">
	SELECT B.BOARD_NO, B.WRITING_TYPE, M.NICK_NAME, B.BOARD_DATE, B.BOARD_COUNT
	FROM BOARD B
	JOIN MEMBER M ON(B.MEMBER_NO = M.MEMBER_NO)
	WHERE B.WRITING_TYPE = 15
	ORDER BY B.BOARD_NO DESC
	</select> -->
	
</mapper> 
  